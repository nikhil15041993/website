pipeline {
    stages {
        stage('git-int') {
            agent { 
                label "testing"
                 echo "testing"
                  }
             steps {
                echo "git int"
             }
            
        }
        
    }
}
